ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.zenthek.fitzenio.rest.ApplicationKt.module ]
    }
}

app {
    projectId = ${?PROJECT_ID}
}
